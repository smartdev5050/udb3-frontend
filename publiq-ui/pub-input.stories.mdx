import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubInput from './pub-input.vue';

<Meta title="Components/pub-input" component={PubInput} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubInput },
    template: '<pub-input v-bind="$props" />',
  };
};

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      name="unique-name"
      placeholder: 'Type something',
      label: 'Custom input',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-input placeholder="Type something" label="Custom input" />
```

<ArgsTable story="Default" />
