import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubSpinner from './pub-spinner';
import PubButton from './pub-button';

<Meta title="Components/pub-spinner" component={PubSpinner} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubSpinner },
    template: '<pub-spinner v-bind="$props" />',
  };
};

# Variants

## On Page

<Canvas>
  <Story name="OnPage">{Template.bind({})}</Story>
</Canvas>

```javascript
<pub-spinner variant="onPage" />
```

export const TemplateInButton = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubSpinner, PubButton },
    template: '<pub-button><pub-spinner v-bind="$props" /></pub-button>',
  };
};

## In Button

<Canvas>
  <Story name="InButton" args={{ variant: 'inButton' }}>
    {TemplateInButton.bind({})}
  </Story>
</Canvas>

```javascript
<pub-button>
  <pub-spinner variant="inButton" />
</pub-button>
```

<ArgsTable story="OnPage" />
