import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubSpinner from './pub-spinner';
import PubButton from './pub-button';

<Meta title="Components/pub-spinner" component={PubSpinner} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubSpinner },
    template: '<pub-spinner v-bind="$props" />',
  };
};

# Variants

## Primary

<Canvas>
  <Story name="Primary">{Template.bind({})}</Story>
</Canvas>

```javascript
<pub-spinner variant="primary" />
```

## Light

<Canvas>
  <Story name="Light" args={{ variant: 'light' }}>
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-spinner variant="light" />
```

<ArgsTable story="Primary" />

# Use the pub-spinner in pub-button

export const TemplateInButton = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubSpinner, PubButton },
    template: '<pub-button v-bind="$props">Click me</pub-button>',
  };
};

<Canvas>
  <Story name="InButton" args={{ loading: true }}>
    {TemplateInButton.bind({})}
  </Story>
</Canvas>

```javascript
<pub-button :loading="true">Click me</pub-button>
```

<ArgsTable story="InButton" />
