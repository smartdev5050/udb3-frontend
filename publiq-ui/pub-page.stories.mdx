import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubPage from './pub-page.vue';

<Meta title="Components/pub-page" component={PubPage} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubPage },
    template: '<pub-page v-bind="$props">Page content</pub-page>',
  };
};

## Default

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

```javascript
<pub-page>Page content</pub-page>
```

<ArgsTable story="Default" />
