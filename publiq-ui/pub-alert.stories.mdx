import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubAlert from './pub-alert.vue';

<Meta title="Components/pub-alert" component={PubAlert} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubAlert },
    template: '<pub-alert v-bind="$props">Lorem ipsum</pub-alert>',
  };
};

export const commonArgTypes = {
  variant: {
    control: {
      type: false,
    },
  },
};

# Variants

## Info

<Canvas>
  <Story
    name="Info"
    args={{ visible: true, variant: 'info' }}
    argTypes={commonArgTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-alert variant="info">Lorem Ipsum</pub-alert>
```

<ArgsTable story="Info" />

## Success

<Canvas>
  <Story
    name="Success"
    args={{ visible: true, variant: 'success' }}
    argTypes={commonArgTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-alert variant="success">Lorem Ipsum</pub-alert>
```

<ArgsTable story="Success" />

## Warning

<Canvas>
  <Story
    name="Warning"
    args={{ visible: true, variant: 'warning' }}
    argTypes={commonArgTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-alert variant="warning">Lorem Ipsum</pub-alert>
```

<ArgsTable story="Warning" />

## Danger

<Canvas>
  <Story
    name="Danger"
    args={{ visible: true, variant: 'danger' }}
    argTypes={commonArgTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-alert variant="danger">Lorem Ipsum</pub-alert>
```

<ArgsTable story="Danger" />
