import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubLoadingSpinner from './pub-loading-spinner';
import PubButton from './pub-button';

<Meta title="Components/pub-loading-spinner" component={PubLoadingSpinner} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubLoadingSpinner },
    template: '<pub-loading-spinner v-bind="$props" />',
  };
};

# Variants

## On Page

<Canvas>
  <Story name="OnPage">{Template.bind({})}</Story>
</Canvas>

```javascript
<pub-loading-spinner variant="onPage" />
```

export const TemplateInButton = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubLoadingSpinner, PubButton },
    template:
      '<pub-button><pub-loading-spinner v-bind="$props" /></pub-button>',
  };
};

## In Button

<Canvas>
  <Story name="InButton" args={{ variant: 'inButton' }}>
    {TemplateInButton.bind({})}
  </Story>
</Canvas>

```javascript
<pub-button>
  <pub-loading-spinner variant="inButton" />
</pub-button>
```

<ArgsTable story="OnPage" />
