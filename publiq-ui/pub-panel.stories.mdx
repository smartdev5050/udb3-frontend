import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubPanel from './pub-panel.vue';

<Meta title="pub-panel" component={PubPanel} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubPanel },
    template: '<pub-panel v-bind="$props">Panel content</pub-panel>',
  };
};

## Default

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

```javascript
<pub-panel>Panel content</pub-panel>
```

<ArgsTable story="Default" />
