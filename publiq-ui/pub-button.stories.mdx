import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubButton from './pub-button.vue';

<Meta title="Components/pub-button" component={PubButton} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubButton },
    template: '<pub-button v-bind="$props">Click me</pub-button>',
  };
};

export const commonArguments = {
  disabled: false,
};

export const commonArgTypes = {
  variant: {
    control: {
      disable: true,
    },
  },
};

# Variants

## Primary

<Canvas>
  <Story
    name="Primary"
    args={{ ...commonArguments, variant: 'primary' }}
    argTypes={commonArgTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-button variant="primary">Click me</pub-button>
```

<ArgsTable story="Primary" />

## Secondary

<Canvas>
  <Story
    name="Secondary"
    args={{ ...commonArguments, variant: 'secondary' }}
    argTypes={commonArgTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-button variant="secondary">Click me</pub-button>
```

<ArgsTable story="Secondary" />

## Success

<Canvas>
  <Story
    name="Success"
    args={{ ...commonArguments, variant: 'success' }}
    argTypes={commonArgTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-button variant="success">Click me</pub-button>
```

<ArgsTable story="Success" />

## Danger

<Canvas>
  <Story
    name="Danger"
    args={{ ...commonArguments, variant: 'danger' }}
    argTypes={commonArgTypes}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-button variant="danger">Click me</pub-button>
```

<ArgsTable story="Danger" />
