import {
  Meta,
  Story,
  Canvas,
  Props,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import PubModal, { ModalVariant } from './pub-modal.vue';

export const parameters = {
  previewTabs: {
    'storybook/docs/panel': {
      hidden: true,
    },
  },
};

<Meta title="pub-modal" component={PubModal} parameters={parameters} />

export const Template = (args, { argTypes }) => {
  console.log({ args }, { argTypes });
  return {
    props: Object.keys(argTypes),
    components: { PubModal },
    template: '<pub-modal v-bind="$props">Lorem ipsum</pub-modal>',
  };
};

# Variants

## Default

<Canvas>
  <Story
    name="Default"
    args={{
      title: 'Default modal',
      isVisible: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-modal
  title="Default modal"
  is-visible="true"
  :variant="ModalVariant.DEFAULT"
>Lorem ipsum</pub-modal>
```

<ArgsTable story="Default" />

## With buttons

<Canvas>
  <Story
    name="WithButtons"
    args={{
      title: 'Modal with buttons',
      isVisible: true,
      variant: ModalVariant.WITH_BUTTONS,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-modal title="Modal with buttons" is-visible="true" :variant="ModalVariant.WITH_BUTTONS" >Lorem ipsum</pub-modal>
```

<ArgsTable story="WithButtons" />
