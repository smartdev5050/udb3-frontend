import {
  Meta,
  Story,
  Canvas,
  Props,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import PubModalQuestion from './pub-modal-question';
import PubButton from '@/publiq-ui/pub-button';

<Meta
  title="Components/pub-modal/pub-modal-question"
  component={PubModalQuestion}
/>

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    data() {
      return {
        isVisible: false,
      };
    },
    components: { PubModalQuestion, PubButton },
    methods: {
      handleClick() {
        this.isVisible = true;
      },
      handleHide() {
        this.isVisible = false;
      },
    },
    template:
      '<div><pub-button @click="handleClick">Open modal</pub-button><pub-modal-question v-bind="$props" :visible="isVisible" @hide="handleHide">Modal content</pub-modal-question></div>',
  };
};

**
This component is a wrapper around the [b-modal](https://bootstrap-vue.org/docs/components/modal) component from bootstrap-vue.
**

<Canvas>
  <Story
    name="Default"
    args={{
      title: 'Question modal',
    }}
    argTypes={{
      visible: {
        control: {
          type: false,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-modal-question
  title="Question modal"
  :visible="isVisible"
  @hide="handleHide"
>
  Modal content
</pub-modal-question>
```

<ArgsTable story="Default" />
