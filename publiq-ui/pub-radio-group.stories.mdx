import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubRadioGroup from './pub-radio-group.vue';

<Meta title="Components/pub-radio-group" component={PubRadioGroup} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubRadioGroup },
    data() {
      return {
        selected: '',
      };
    },
    template:
      '<div><pub-radio-group v-bind="$props" v-model="selected" /><br/><span>Selected city: {{selected}}</span></div>',
  };
};

export const commonArgs = {
  name: 'city',
  groupLabel: 'Select a city:',
  labels: ['Rome', 'Paris', 'Prague'],
  values: ['rome', 'paris', 'prague'],
};

## Default

<Canvas>
  <Story name="Default" args={commonArgs}>
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-radio-group
  v-model="selected"
  name="city"
  groupLabel="Select a city:"
  :labels="['Rome', 'Paris', 'Prague']",
  :values="['rome', 'paris', 'prague']",
/>
<span>Selected city: {{selected}}</span>
```

<ArgsTable story="Default" />
