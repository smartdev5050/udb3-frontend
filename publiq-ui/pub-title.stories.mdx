import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubTitle from './pub-title';

<Meta title="Components/pub-title" component={PubTitle} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubTitle },
    template: '<pub-title v-bind="$props">Title</pub-title>',
  };
};

## Size 1

<Canvas>
  <Story
    name="Size1"
    args={{ size: 1 }}
    argTypes={{ size: { control: { type: false } } }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-title size="1">Title</pub-title>
```

## Size 2 (Default)

<Canvas>
  <Story name="Size2" args={{ size: 2 }}>
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-title size="2">Title</pub-title>
```

<ArgsTable story="Size1" />
