import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubPagination from './pub-pagination.vue';

<Meta title="pub-pagination" component={PubPagination} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubPagination },
    template: '<pub-pagination v-bind="$props" />',
  };
};

export const commonArgs = {
  rows: 50,
  prevText: 'Previous one',
  nextText: 'Next one',
};

export const commonArgTypes = {
  prevText: {
    defaultValue: "this.$t('pagination.previous')",
  },
  nextText: {
    defaultValue: "this.$t('pagination.next')",
  },
};

## Default

<Canvas>
  <Story name="Default" args={commonArgs} argTypes={commonArgTypes}>
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-pagination rows="50" prev-text="Previous one" next-text="Previous one" />
```

<ArgsTable story="Default" />
