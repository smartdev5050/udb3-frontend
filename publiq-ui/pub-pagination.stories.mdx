import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubPagination from './pub-pagination.vue';

<Meta title="Components/pub-pagination" component={PubPagination} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubPagination },
    template: '<pub-pagination v-bind="$props" />',
  };
};

export const commonArgs = {
  total: 50,
  prevText: 'Previous one',
  nextText: 'Next one',
};

export const commonArgTypes = {
  prevText: {
    table: {
      defaultValue: { summary: '$t(pagination.previous)' },
    },
  },
  nextText: {
    table: {
      defaultValue: { summary: '$t(pagination.next)' },
    },
  },
};

## Default

<Canvas>
  <Story name="Default" args={commonArgs} argTypes={commonArgTypes}>
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-pagination total="50" prev-text="Previous one" next-text="Previous one" />
```

<ArgsTable story="Default" />
