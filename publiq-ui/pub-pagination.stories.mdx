import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import PubPagination from './pub-pagination.vue';

<Meta title="Components/pub-pagination" component={PubPagination} />

export const Template = (args, { argTypes }) => {
  return {
    props: Object.keys(argTypes),
    components: { PubPagination },
    template: '<pub-pagination v-bind="$props" />',
  };
};

export const commonArgs = {
  total: 50,
  prevText: 'Previous one',
  nextText: 'Next one',
};

## Default

<Canvas>
  <Story name="Default" args={commonArgs}>
    {Template.bind({})}
  </Story>
</Canvas>

```javascript
<pub-pagination
    :current-page="currentPage"
    :total="totalItems"
    :per-page="perPage"
    prev-text="Previous one"
    next-text="Previous one"
    @changePage="handleChangePage"
/>
```

<ArgsTable story="Default" />
