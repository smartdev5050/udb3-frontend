import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { TypeaheadWithLabel } from './TypeaheadWithLabel.js';

<Meta title="Components/TypeaheadWithLabel" />

export const cities = [
  { id: 1, name: 'Rome' },
  { id: 2, name: 'Paris' },
  { id: 3, name: 'Prague' },
];

export const commonArgs = {
  label: 'Search for a city:',
  options: cities,
  labelKey: (city) => city.name,
};

export const commonArgTypes = {
  onInputChange: { action: 'onInputChange' },
  onChange: { action: 'onChange' },
  onSearch: { action: 'onSearch' },
};

<Canvas>
  <Story name="Default" args={commonArgs} argTypes={commonArgTypes}>
    {(args) => <TypeaheadWithLabel {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Default" />
