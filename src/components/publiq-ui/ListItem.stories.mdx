import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { ListItem } from './ListItem.js';

<Meta title="Components/ListItem" />

export const argTypes = { onClick: { action: 'onClick' } };

<Canvas>
  <Story name="Default" args={{ children: 'Item' }} argTypes={argTypes}>
    {(args) => <ListItem {...args} />}
  </Story>
</Canvas>
