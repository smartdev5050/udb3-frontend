import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Button, ButtonVariants } from './Button.js';

<Meta title="Components/Button" />

export const commonArgs = {
  disabled: false,
};

export const commonArgTypes = { onClick: { action: 'onClick' } };

# Variants

## Primary

<Canvas>
  <Story
    name="Primary"
    args={{
      variant: ButtonVariants.PRIMARY,
      children: 'Primary',
      ...commonArgs,
    }}
    argTypes={commonArgTypes}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Primary" />

## Secondary

<Canvas>
  <Story
    name="Secondary"
    args={{
      variant: ButtonVariants.SECONDARY,
      children: 'Secondary',
      ...commonArgs,
    }}
    argTypes={commonArgTypes}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Secondary" />

## Success

<Canvas>
  <Story
    name="Success"
    args={{
      variant: ButtonVariants.SUCCESS,
      children: 'Success',
      ...commonArgs,
    }}
    argTypes={commonArgTypes}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Success" />

## Danger

<Canvas>
  <Story
    name="Danger"
    args={{
      variant: ButtonVariants.DANGER,
      children: 'Danger',
      ...commonArgs,
    }}
    argTypes={commonArgTypes}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Danger" />
