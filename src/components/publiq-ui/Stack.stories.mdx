import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Box } from './Box.js';
import styled from 'styled-components';
import { Stack } from './Stack.js';

<Meta title="Primitives/Stack" />

export const CustomBox = styled(Box)`
  background-color: #004f94;
  width: 300px;
  height: 100px;
  color: #fff;
`;

export const commonArgs = {
  spacing: 2,
};

export const boxProps = {
  padding: 3,
};

<Canvas>
  <Story
    name="Default"
    args={{
      ...commonArgs,
    }}
  >
    {(args) => (
      <Stack {...args}>
        <CustomBox {...boxProps}>A Box</CustomBox>
        <CustomBox {...boxProps}>Another Box</CustomBox>
        <CustomBox {...boxProps}>And yet another Box</CustomBox>
      </Stack>
    )}
  </Story>
</Canvas>

<ArgsTable story="Default" />

<Canvas>
  <Story
    name="Nested"
    args={{
      ...commonArgs,
    }}
  >
    {(args) => (
      <Stack {...args}>
        <CustomBox {...boxProps}>A Box</CustomBox>
        <Stack spacing={5}>
          <CustomBox {...boxProps}>Another Box</CustomBox>
          <CustomBox {...boxProps}>And yet another Box</CustomBox>
        </Stack>
      </Stack>
    )}
  </Story>
</Canvas>

<ArgsTable story="Nested" />

<Canvas>
  <Story
    name="ToInlineWithBoolean"
    args={{
      ...commonArgs,
      inlineOn: true,
    }}
  >
    {(args) => (
      <Stack {...args}>
        <CustomBox {...boxProps}>A Box</CustomBox>
        <CustomBox {...boxProps}>Another Box</CustomBox>
      </Stack>
    )}
  </Story>
</Canvas>

<ArgsTable story="ToInlineWithQuery" />

<Canvas>
  <Story
    name="ToInlineWithQuery"
    args={{
      ...commonArgs,
      inlineOn: 's',
    }}
  >
    {(args) => (
      <Stack {...args}>
        <CustomBox {...boxProps}>A Box</CustomBox>
        <CustomBox {...boxProps}>Another Box</CustomBox>
      </Stack>
    )}
  </Story>
</Canvas>

<ArgsTable story="ToInlineWithQuery" />
