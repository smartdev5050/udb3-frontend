import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Icon, IconVariants } from './Icon.js';

<Meta title="Components/Icon" />

export const argTypes = {
  variant: {
    control: {
      type: 'select',
      options: [...Object.values(IconVariants)],
    },
  },
};

<Canvas>
  <Story
    name="Default"
    args={{ variant: IconVariants.USER }}
    argTypes={argTypes}
  >
    {(args) => <Icon {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Default" />
