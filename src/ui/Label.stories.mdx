import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Label, LabelVariants } from './Label';

<Meta title="Components/Label" component={Label} />

<Canvas>
  <Story
    name="Normal"
    args={{ children: 'A form label', variant: LabelVariants.NORMAL }}
  >
    {(args) => <Label {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Normal" />

<Canvas>
  <Story
    name="Bold"
    args={{ children: 'A form label', variant: LabelVariants.BOLD }}
  >
    {(args) => <Label {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Bold" />
