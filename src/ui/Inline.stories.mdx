import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Box } from './Box.js';
import styled from 'styled-components';
import { Inline } from './Inline.js';
import { Breakpoints } from '@/ui/theme';

<Meta title="Primitives/Inline" component={Inline} />

export const CustomBox = styled(Box)`
  background-color: #004f94;
  width: 300px;
  height: 100px;
  color: #fff;
`;

export const commonArgs = {
  spacing: 2,
};

export const boxProps = {
  padding: 3,
};

<Canvas>
  <Story
    name="Default"
    args={{
      ...commonArgs,
    }}
  >
    {(args) => (
      <Inline {...args}>
        <CustomBox {...boxProps}>A Box</CustomBox>
        <CustomBox {...boxProps}>Another Box</CustomBox>
        <CustomBox {...boxProps}>And yet another Box</CustomBox>
      </Inline>
    )}
  </Story>
</Canvas>

<ArgsTable story="Default" />

<Canvas>
  <Story
    name="Nested"
    args={{
      ...commonArgs,
    }}
  >
    {(args) => (
      <Inline {...args}>
        <CustomBox {...boxProps}>A Box</CustomBox>
        <Inline spacing={5}>
          <CustomBox {...boxProps}>Another Box</CustomBox>
          <CustomBox {...boxProps}>And yet another Box</CustomBox>
        </Inline>
      </Inline>
    )}
  </Story>
</Canvas>

<ArgsTable story="Nested" />

<Canvas>
  <Story
    name="ToStackWithQuery"
    args={{
      ...commonArgs,
      stackOn: Breakpoints.S,
    }}
  >
    {(args) => (
      <Inline {...args}>
        <CustomBox {...boxProps}>A Box</CustomBox>
        <CustomBox {...boxProps}>Another Box</CustomBox>
      </Inline>
    )}
  </Story>
</Canvas>

<ArgsTable story="ToStackWithQuery" />
