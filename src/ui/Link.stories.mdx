import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Link, LinkVariants } from './Link';
import { Icon, Icons } from './Icon';
import { Text } from '@/ui/Text';
import { Inline } from '@/ui/Inline';

<Meta title="Components/Link" component={Link} />

<Canvas>
  <Story name="Internal" args={{ children: 'internal', href: '/test' }}>
    {(args) => (
      <Text>
        This is an <Link {...args} /> link
      </Text>
    )}
  </Story>
</Canvas>

<ArgsTable story="Internal" />

<Canvas>
  <Story
    name="Unstyled Internal"
    args={{
      children: 'unstyled internal',
      href: '/test',
      variant: LinkVariants.UNSTYLED,
    }}
  >
    {(args) => <Link {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Unstyled Internal" />

<Canvas>
  <Story
    name="External Link"
    args={{ children: 'external', href: 'http://www.google.com' }}
  >
    {(args) => <Link {...args} />}
  </Story>
</Canvas>

<ArgsTable story="External Link" />

<Canvas>
  <Story
    name="Unstyled External"
    args={{
      children: 'unstyled external',
      href: 'http://www.google.com',
      variant: LinkVariants.UNSTYLED,
    }}
  >
    {(args) => <Link {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Unstyled External" />

## Custom Children

<Canvas>
  <Story
    name="Custom Children"
    args={{
      customChildren: true,
      children: <Icon name={Icons.TIMES} />,
      href: 'http://www.google.com',
      variant: LinkVariants.UNSTYLED,
    }}
  >
    {(args) => <Link {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Custom Children" />

<Canvas>
  <Story
    name="As button"
    args={{
      href: '/?path=/story/introduction-design-principles--page',
    }}
  >
    {(args) => (
      <Inline spacing={3}>
        <Link {...args} variant={LinkVariants.BUTTON_PRIMARY}>
          Primary
        </Link>
        <Link {...args} variant={LinkVariants.BUTTON_SECONDARY}>
          Secondary
        </Link>
        <Link {...args} variant={LinkVariants.BUTTON_SUCCESS}>
          Success
        </Link>
        <Link {...args} variant={LinkVariants.BUTTON_DANGER}>
          Danger
        </Link>
      </Inline>
    )}
  </Story>
</Canvas>

<ArgsTable story="As button" />
