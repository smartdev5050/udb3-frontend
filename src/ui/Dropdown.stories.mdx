import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Dropdown, DropDownVariants } from './Dropdown';
import { action } from '@storybook/addon-actions';
import { Button } from '@/ui/Button';
import { Link } from '@/ui/Link';

<Meta title="Components/Dropdown" component={Dropdown} />

# Variants

## With button

<Canvas>
  <Story
    name="With button"
    args={{
      variant: DropDownVariants.PRIMARY,
      isSplit: true,
    }}
  >
    {(args) => (
      <Dropdown {...args}>
        <Button onClick={action('onClick')} variant={args.variant}>
          Primary action
        </Button>
        <Dropdown.Item href="#example-href-1">Example href 1</Dropdown.Item>
        <Dropdown.Item onClick={() => action('onClick')('Example onClick 1')}>
          Example onClick 1
        </Dropdown.Item>
        <Dropdown.Divider />
        <Dropdown.Item href="#example-href-2">Example href 2</Dropdown.Item>
        <Dropdown.Item onClick={() => action('onClick')('Example onClick 2')}>
          Example onClick 2
        </Dropdown.Item>
      </Dropdown>
    )}
  </Story>
</Canvas>

<ArgsTable story="With button" />

## With link

<Canvas>
  <Story
    name="With link"
    args={{
      variant: DropDownVariants.PRIMARY,
      isSplit: true,
    }}
  >
    {(args) => (
      <Dropdown {...args}>
        <Link href="#example" variant={args.variant}>
          Primary action
        </Link>
        <Dropdown.Item href="#example-href-1">Example href 1</Dropdown.Item>
        <Dropdown.Item onClick={() => action('onClick')('Example onClick 1')}>
          Example onClick 1
        </Dropdown.Item>
        <Dropdown.Divider />
        <Dropdown.Item href="#example-href-2">Example href 2</Dropdown.Item>
        <Dropdown.Item onClick={() => action('onClick')('Example onClick 2')}>
          Example onClick 2
        </Dropdown.Item>
      </Dropdown>
    )}
  </Story>
</Canvas>

<ArgsTable story="With link" />
