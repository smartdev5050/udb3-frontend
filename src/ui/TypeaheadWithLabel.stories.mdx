import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { TypeaheadWithLabel } from './TypeaheadWithLabel';

<Meta title="Components/TypeaheadWithLabel" component={TypeaheadWithLabel} />

export const cities = [
  { id: 1, name: 'Rome' },
  { id: 2, name: 'Paris' },
  { id: 3, name: 'Prague' },
];

export const commonArgs = {
  id: 'typeahead-test',
  label: 'Search for a city:',
  options: cities,
  labelKey: (city) => city.name,
};

<Canvas>
  <Story name="Default" args={commonArgs}>
    {(args) => <TypeaheadWithLabel {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Default" />
