import { useState } from 'react';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { CheckboxWithLabel } from './CheckboxWithLabel';

<Meta title="Components/CheckboxWithLabel" component={CheckboxWithLabel} />

<Canvas>
  <Story
    name="Default"
    args={{
      id: '123',
      name: 'my-checkbox',
      checked: false,
      disabled: false,
      children: 'Click me',
    }}
  >
    {(args) => {
      const [checked, setChecked] = useState(false);
      return (
        <CheckboxWithLabel
          {...args}
          checked={checked}
          onToggle={(event) => {
            args.onToggle(event);
            setChecked(!checked);
          }}
        />
      );
    }}
  </Story>
</Canvas>

<ArgsTable story="Default" />
