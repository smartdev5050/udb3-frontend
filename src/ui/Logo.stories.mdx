import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Logo, LogoVariants } from './Logo';

export const commonArgs = {
  color: 'black',
};

export const commonArgTypes = {
  width: { type: { name: 'number' } },
};

<Meta title="Components/Logo" component={Logo} />

<Canvas>
  <Story name="Default" args={commonArgs} argTypes={commonArgTypes}>
    {(args) => <Logo {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Default" />

<Canvas>
  <Story
    name="Mobile"
    args={{ ...commonArgs, variant: LogoVariants.MOBILE }}
    argTypes={commonArgTypes}
  >
    {(args) => <Logo {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Mobile" />
