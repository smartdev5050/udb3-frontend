import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Toast, ToastVariants } from './Toast';
const defaultArgs = { body: 'My Super Mega Long Toast Message' };

<Meta title="Components/Toast" component={Toast} />

# Variants

## Primary

<Canvas>
  <Story
    name="Primary"
    args={{ variant: ToastVariants.PRIMARY, ...defaultArgs }}
  >
    {(args) => <Toast {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Primary" />

## Success

<Canvas>
  <Story
    name="Success"
    args={{ variant: ToastVariants.SUCCESS, ...defaultArgs }}
  >
    {(args) => <Toast {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Success" />

## Danger

<Canvas>
  <Story name="Danger" args={{ variant: ToastVariants.DANGER, ...defaultArgs }}>
    {(args) => <Toast {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Danger" />
